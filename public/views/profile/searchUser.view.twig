<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Search up a user</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        input::-webkit-outer-spin-button,
        input::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }
    </style>
</head>
<body style="background-color: #eee;">

{% include 'navBar/navBar.view.twig' %}
{# <div class="container mt-5 d-flex flex-column"> #}



{#    <form> #}
{#        <div class="d-flex" style="padding-left: 0.5em; padding-right: 0.5em"> #}
{#            <input type="text" #}
{#                   placeholder="Search for a user (by id)" #}
{#                   name="id" #}
{#                   style="width: 18em" #}
{#                   class="form-control"> #}
{#            <button type="submit" #}
{#                    class="btn btn-primary" #}
{#                    style="margin-left: 0.2em"> #}
{#                Search #}
{#            </button> #}
{#        </div> #}
{#    </form> #}

{# </div> #}


<section class="vh-100" style="background-color: #eee;">
    <div class="container py-5 h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-md-11 col-xl-4">
                <div>
                    {% for error in errors %}
                        <div class="alert alert-danger" role="alert">
                            {{ error }}
                        </div>
                    {% endfor %}
                </div>
                <div class="card" style="border-radius: 15px;">
                    <div class="card-body text-center">
                        <div style="margin-top: 8em; margin-bottom: 2em">
                            <h4>Search for a user</h4>
                        </div>
                        <div style="margin-bottom: 10em">
                            <form method="POST">
                                <div class="d-flex row" style="padding-left: 0.5em; padding-right: 0.5em">
                                    <div>
                                        <input type="number"
                                               placeholder="Search for a user (by id)"
                                               name="id"
                                               style="width: 100%"
                                               class="form-control"
                                               required>
                                    </div>
                                    <div>
                                        <input type="submit" class="btn" value="Search">
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

</body>
</html>